[package]
name = "smart-bundle-transform"
version = "1.0.0"
edition = "2021"

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }

[profile.release]
lto = true
opt-level = 3
strip = true
panic = "abort"
codegen-units = 1
